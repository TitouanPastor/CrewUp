# Simple image to host the Keycloak theme JAR
FROM alpine:latest

# Copy the theme JAR
COPY crewup-theme.jar /theme/

# Default command (not really used, just for clarity)
CMD ["sh", "-c", "echo 'Theme image ready'"]
