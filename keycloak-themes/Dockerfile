# Image to host the extracted Keycloak theme with favicon
FROM alpine:latest

# Copy the extracted theme directory
COPY crewup-theme-extracted/theme/keycloakify-starter /theme/crewup-theme

# Default command
CMD ["sh", "-c", "echo 'Theme image ready'"]
